@using Microsoft.AspNetCore.Identity
@inject SignInManager<EventuresUser> SignInManager
@inject UserManager<EventuresUser> UserManager
@if (SignInManager.IsSignedIn(User))
{
    <ul class="nav navbar-nav">
        <li>
            <div class="dropdown">
                <div class="btn dropdown-toggle nav-link font-weight-bold" id="dropdownMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Events
                </div>
                <div class="dropdown-menu" aria-labelledby="dropdownMenu">
                    <a asp-controller="Event" asp-action="All" class="dropdown-item">All Events</a>
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-controller="Event" asp-action="Create" class="dropdown-item">Create Event</a>
                    }
                </div>
            </div>
        </li>
    </ul>
}
